# Cubby — downloads

Cubby is a clipboard and screenshot manager for macOS.

This repository holds only the released builds. Every version lives under the
single `downloads` release rather than one release per version, because the
update feed applies one address to all of them — a tag per version would break
the download link for anyone still on an older build.

The app's source lives elsewhere.
